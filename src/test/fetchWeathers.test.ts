import { fetchCurrentWeather } from "../storage/thunks/fetchWeathers"


/*test('', async () => {
    const thunk = fetchCurrentWeather('')
    const dispatchMock = jest.fn()

    await thunk(dispatchMock)

    expect(dispatchMock).toBeCalledTimes(1)

})*/
